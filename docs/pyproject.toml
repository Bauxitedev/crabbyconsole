[project]
name = "docs"
version = "0.1.0"
description = "Documentation for CrabbyConsole"
readme = "README.md"
requires-python = ">=3.14"
dependencies = []

[project.scripts]
docs = "docs:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"

[dependency-groups]
dev = ["zensical==0.0.60"]
